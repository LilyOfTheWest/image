#include "propertiestransfo.h"

PropertiesTransfo::PropertiesTransfo(QObject *parent) : QObject(parent)
{

    enum (moyenneur,binomial);
    int ordre_convolution;
    double alpha_rehaussement;

}

